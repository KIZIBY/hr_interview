# Stage 2 — Audit Team, отчёт

Дата: 2026-07-12 · Audit Director: оркестратор (Opus) · субагенты (Sonnet, параллельно): Security Reviewer, Architecture Reviewer, Frontend+Performance Reviewer (роли объединены — страница статическая), Content/Domain Reviewer (вместо шаблонного AI Reviewer — фича контентная, AI-инфраструктуры нет).

## Счётчик findings

**CRITICAL: 6 · WARNING: 18 · INFO: 13** — все CRITICAL и ключевые WARNING закрыты правками spec.md и требованиями plan.md до гейта (трассировка — в plan.md, секция «Findings аудита → план»).

### CRITICAL (разрешены)
1. (Frontend F-1) Эмодзи 🟢/🔴 в рендере сигналов запрещены → CSS-кружки токенами X5 (uw-1.4).
2. (Frontend F-2) `pre` без overflow-x даёт горизонтальный скролл страницы (строки эталонов до 97 символов) → overflow локализован в pre (uw-1.5).
3. (Arch R-1) NFR-1 ≤2500 слов vs объём FR-3/FR-4 — достижимо только с редакционным constraint (реплики, не параграфы; `wc -w` перед коммитом) (uw-2.6).
4. (Content C-1) Сегмент 55–60 «вопросы кандидата + next step» не имел контента в FR → FR-3 дополнен.
5. (Content C-2) Отсутствовали «речевые мосты» для слабого интервьюера (молчит / ушёл не туда / не понял ответ) → FR-4 дополнен тремя дословными репликами.
6. (Content C-3) Риск превращения рубрик discussion/design задач в чеклист дословных совпадений; пороги DBG-задач спрятаны в evaluationNotes → FR-5 disclaimer-блок + FR-7 порог на карточку.

### WARNING (ключевые, все учтены)
- Security: tasks.json как принятый риск зафиксирован явно в A&A; правило «пульт кандидату не демонстрируется никогда»; `rel="noopener"` на ссылках; требование `esc()` внесено в FR-5; print-плашка конфиденциальности (FR-2).
- Architecture: контракт якорей `task-{id}` фиксируется в CLAUDE.md; SYNC-guard (комментарии-контракты + console.warn) для дублированных SEGMENTS/CRITERIA; Units of Work разделены на код/контент; esc()/mdLite() — копия в inline-script (render-util.js не создавать).
- Frontend: `--topbar-h` + `scroll-margin-top` для якорей под sticky-шапкой; skip-link; иерархия h1→h4 без пропусков; print-бюджет оценочного листа (10pt, page-break-before); Lucide при использовании — только локально/inline.
- Content: правило слабой зоны 48–55; выбор задачи 34–48 «под пробел сигнала»; скрипт 0–5 двумя ходами; маркеры 1/3/4 из сигналов, не из общих соображений; стоп-строки поимённо; блок «Памятка» 6 пунктов дословно; правило перехода между зонами.

### Вне scope — отдельный тикет после approve
- Эмодзи 🟢/🔴 в interviewer.html (нарушение X5, уже зафиксировано QA-отчётом) — минорный тикет на замену CSS-маркерами.

## Constitution Check (по docs/constitution-check.md)

`docs/constitution.md` в проекте **отсутствует** (есть только `docs/constitution.template.md` — шаблон не инстанциирован). Оценить вход по инвариантам I-1…I-16 невозможно; по процедуре (I-13: не угадывать) все 16 строк — **NEEDS-INFO** с требованием: инстанциировать устав из шаблона отдельной задачей владельца (вне scope этой фичи).

**Итог: MUST-FLAG: 0 · SHOULD-FLAG: 0 · NEEDS-INFO: 16.**

Блокировки нет (MUST-FLAG: 0); NEEDS-INFO эскалируется владельцу как отдельное решение.

## Вердикт Audit Director

План к утверждению готов: CRITICAL — 0 открытых, спека и план согласованы, Units of Work заданы (`uw-1-code`, `uw-2-content`), тестирование определено (guide-smoke.mjs + регресс 44/44 + ручной print/тайминг-чек).
