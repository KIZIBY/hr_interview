# Requirements Matrix — Spec 001 · guide.html

- Дата аудита: 2026-07-12
- Ревьюер: Requirements Validator, stage-4 (Quality)
- Источники: `specs/001-interviewer-guide/spec.md`, `guide.html`, `interviewer.html`, `interviewer.js`, `CLAUDE.md`, `docs/qa/2026-07-12-e2e/guide-smoke.mjs`

---

## Функциональные требования (FR)

| Требование | Статус | Доказательство | Проверка |
|---|---|---|---|
| **FR-1** Standalone, корень, X5 DS первым импортом, X5 Sans, --x5-ink, логотип снизу слева, без эмодзи, sentence case | DONE | `guide.html:8` `<link rel="stylesheet" href="X5_Group_Design_System/colors_and_type.css" />`; `guide.html:478` `<img class="brand-logo" src="X5_Group_Design_System/assets/logos/x5-logo-dark.svg" ...`; body `color: var(--x5-ink)`, font-family `"X5 Sans"` | GS-09 (X5 Sans + цвет тела), GS-05 (нет эмодзи 🟢/🔴), код |
| **FR-2** Баннер — первый видимый элемент; инструкция отдельного окна; print-плашка «конфиденциальный — не передавать кандидатам» НЕ скрыта в @media print | PARTIAL | Баннер `.guard-banner` существует (`guide.html:215–220`), инструкция отдельного окна присутствует (`guide.html:218` «Держите гайд в отдельном окне браузера»). **НО:** @media print (`guide.html:191–200`) не содержит отдельной плашки «документ конфиденциальный — не передавать кандидатам»; баннер сохраняется (`border: 1px solid #999`), но сам текст баннера не содержит отдельной формулировки «документ конфиденциальный» — только «Только для интервьюера. Документ содержит эталонные решения… не передавать кандидатам». Формулировка близкая, но спека требует дословно «документ конфиденциальный — не передавать кандидатам» именно как print-плашку | GS-10 (баннер первый), GS-12 (@media print существует); **print-плашка-дословно — не проверено автотестом** |
| **FR-3** Таймбокс-таблица 6 сегментов с комментарием SYNC-CONTRACT; скрипт первых 5 минут (2 смысловых хода); правило 34–48 (задача под пробел, не по порядку); правило 48–55 (слабая зона → туда); 55–60 дежурные ответы; правило перехода зон | DONE | Таблица: `guide.html:260–269` (6 строк, 0–5…55–60); SYNC-CONTRACT: `guide.html:258–259`; скрипт 5 минут — «Ход 1» и «Ход 2»: `guide.html:274–281`; правило 34–48: `guide.html:284–287`; правило 48–55: `guide.html:289–292`; дежурные реплики 55–60: `guide.html:296–301`; callout с правилом перехода: `guide.html:303–304` | Код (ручной просмотр), SYNC-guard в guide.html сверяет с interviewer.js |
| **FR-4** Пять зон проверки: что проверяем, антивайб-зонды (3 шаблона), маркеры 1/3/4, стоп-сигналы; «Речевые мосты» — три реплики дословно из спеки | PARTIAL | Пять зон с «что проверяем», антивайб-зондами, маркерами 1/3/4: `guide.html:310–383`. Речевые мосты: `guide.html:396–401`. Реплика 1: «Можешь думать вслух — мне интересен ход рассуждений.» (спека: «можешь думать вслух — мне интересен ход рассуждений»). Реплика 2: «А если посмотреть со стороны надёжности / состояния?» (спека: «а если посмотреть со стороны надёжности/состояния?»). Реплика 3: «Объясни, как объяснял бы коллеге, который с этим не работал.» (спека: «объясни, как объяснял бы коллеге, который с этим не работал»). **НО:** в спека-зонах требуются также стоп-сигналы зоны отдельным блоком — в гайде стоп-сигналы как зональные перечни не выделены явно; маркер «звучит как 1» совпадает со стоп-сигналом только неявно. Формально: стоп-сигналы отдельным блоком в каждой зоне отсутствуют | Код (ручной просмотр); автотест не покрывает точность формулировок |
| **FR-5** Памятка: карточки из tasks.json через fetch; якорь id="task-ID"; esc() на ВСЕХ строковых полях tasks.json при вставке в innerHTML | DONE | fetch: `guide.html:625`; карточки: `guide.html:629–631`; якоря: `guide.html:630` `id="task-' + esc(t.id) + '"`. Функция esc: `guide.html:485–489`. **Проверка всех полей в renderCard:** `t.id` → `esc()` (строка 549), `t.title` → `esc()` (549), `t.category` → `esc(CAT_LABEL[...] \|\| t.category)` (551), `t.difficulty` → `esc()` (552), `t.scoringDimension` → `esc(DIM_LABEL[...])` (553), `t.weight` → числовой литерал (не строка), `t.timeBudgetMin` → числовой литерал, `t.format` → `esc()` (555), `t.mlSpecific` → boolean. `t.prompt` → `mdLite()` (566) которая вызывает `inlineMd()` → `esc()`. `ex.input`, `ex.output` → `esc()` (571). `ex.explanation` → `esc()` (571). `t.buggyVersion` → `esc()` (577). `r.label`, `r.complexity`, `r.notes` → `esc()` (583–586). `t.evaluationNotes` → в ветке bugs: `inlineMd(b)` (строка 597) — esc внутри; в ветке обычная → `mdLite()` (601). Сигналы: `esc(stripSignalEmoji(s))` (608, 610). `t.followUps` → `inlineMd(f)` (616). Прямых `innerHTML = rawField` нет | GS-08 (XSS-guard живые script + pre), GS-03 (id карточек), код |
| **FR-6** Задачи с mlSpecific:true — предупреждение «требует ML-контекста» | DONE | `guide.html:559–561`: `if (t.mlSpecific) { html += '<div class="note-ml">Требует ML-контекста…</div>'; }`. DBG-02 и DBG-03 имеют `mlSpecific: true` по tasks.json | Код; автотест GS-05 косвенно (без эмодзи), прямого автошага нет |
| **FR-7** Debug-задачи с багами в evaluationNotes (DBG-02, DBG-03) — чеклист-список; пороговое число засчитывания на карточке рядом с чеклистом | PARTIAL | Чеклист: `parseBugNotes()` (`guide.html:521–538`) разбирает нумерованный список; `ul.bugcheck` рендерится. `thresholdFrom()` (`guide.html:539–542`) ищет предложения с «наход\|достаточн» и цифрой. DBG-02: evaluationNotes содержит «Достаточно найти хотя бы 3» — но тест `/(наход|достаточн)/i` и `/\d/` должен совпадать. DBG-03: «Достаточно найти хотя бы 3» также. **НО:** порядок: `thresholdFrom` ищет в `bugs.tail`, а `bugs.tail` — текст после последней точки в последнем пункте. DBG-02: последний пункт «8) .cuda() вместо .to(device)…», после точки идёт «Сильный кандидат с ML-бэкграундом находит 5+; средний — 3–4. Задача для кандидатов с опытом PyTorch.» — tail это «Задача для кандидатов с опытом PyTorch.»; threshold-строка не попадает в tail, а остаётся в `last.slice(0, cut)` — значит threshold = '' (пусто). Аналогично DBG-03. Пороговое число на карточку **не выносится** — плашка `.note-threshold` не появляется | Код (ручной анализ parseBugNotes/thresholdFrom + содержимого evaluationNotes); автотест прямо не проверяет |
| **FR-8** Секция «Оценка и вердикт»: 5 критериев с весами, стоп-строки поимённо (CS-фундамент, проектирование агентов, прод-зрелость), шкала 1–4, GO/NO-GO (≥3.0, без «1» в стоп-строках), сквозные сигналы «берём/стоп», блок «Памятка» — 6 пунктов дословно | PARTIAL | Таблица критериев с весами и стоп-строками: `guide.html:446–455`; все 3 стоп-строки поимённо: `guide.html:459`. GO/NO-GO: `guide.html:457–463`. Памятка-6: `guide.html:466–473` — **6 пунктов** соответствуют спеке. **НО:** «сквозные сигналы этапа» — в спека требует блок «берём/стоп» именно в секции «Оценка и вердикт»; в гайде сквозные сигналы выделены в отдельную секцию `#signals` (`guide.html:404–429`), а не внутри `#scoring`. Формально они присутствуют на странице, но не в заданном разделе | Код; автотест не покрывает содержимое секции scoring |
| **FR-9** Якорная навигация: sticky-оглавление (флоу / зоны / задачи / оценка); список задач в TOC памятки | DONE | TOC: `guide.html:223–235` — 6 пунктов (howto, flow, zones, signals, tasks-ref, scoring), `position: sticky`. Список задач в TOC: `toc__tasks` с id `toc-tasks` заполняется скриптом `guide.html:632–635` | Код; GS-07 (якорь task-ALG-01 в зоне видимости) |
| **FR-10** Ссылка «Гайд» в topbar interviewer.html (target="_blank" rel="noopener"); ссылка «Памятка по задаче» → guide.html#task-ID в renderDetails | DONE | topbar: `interviewer.html:237` `<a class="btn" href="guide.html" target="_blank" rel="noopener">Гайд</a>`. renderDetails: `interviewer.js:304` `<a class="guide-link" href="guide.html#task-' + esc(t.id) + '" target="_blank" rel="noopener">памятка по задаче</a>` | GS-11a, GS-11b |
| **FR-11** @media print: скрыть nav/баннер-кнопки, разрывы страниц; секция «Оценка» — один лист A4 | PARTIAL | @media print: `guide.html:191–200`; скрыто: `.topbar, .toc, .skip-link, .brand-logo`. **НО:** `.guard-banner` НЕ скрыт (задан `border: 1px solid #999` — оставлен видимым). По спека баннер остаётся при печати — это соответствует FR-2. `#scoring { page-break-before: always }`. task-card: `break-inside: avoid`. **Кнопки баннера**: в баннере кнопок нет, только текст — окей. **Баннер-кнопки** в смысле .topbar-кнопок скрыты (.topbar скрыт). Секция scoring на отдельной странице (`page-break-before: always`) — соответствует «один лист». Fit одной страницы A4 не проверено автоматом | GS-12 (@media print существует); ручное print-preview не проверено |
| **FR-12** При fetch-ошибке tasks.json — секция памятки показывает ошибку с инструкцией http-сервера; остальные секции работают | DONE | `guide.html:641–646`: catch → `cardsEl.innerHTML = '<div class="card-error">Не удалось загрузить tasks.json… запустите python3 -m http.server 8000 … Остальные разделы гайда работают.</div>'`; `section.removeAttribute('aria-busy')`. Остальные секции — статический HTML, не зависят от fetch | Код; автотест не проверяет сценарий file:// ошибки |

---

## Нефункциональные требования (NFR)

| Требование | Статус | Доказательство | Проверка |
|---|---|---|---|
| **NFR-1** Подготовительная часть ≤ 2500 слов (без памятки задач), читается за ≤ 15 минут | DONE | Секции howto+flow+zones+signals+scoring содержат ~1900 слов (оценка по HTML-тексту без скрипта и без карточек); ниже порога 2500 | Ручная оценка; автотест не покрывает |
| **NFR-2** Один сетевой запрос (tasks.json), рендер ≤ 200 мс, без внешних CDN | PARTIAL | fetch tasks.json: `guide.html:625`; sync-guard также делает fetch('interviewer.js') — второй сетевой запрос (guide.html:651–667). Без внешних CDN: подтверждено (все ресурсы локальные). Рендер: GS-04 проверяет с жёстким порогом 2000 мс; NFR-порог 200 мс — только лог | GS-04 (рендер), GS-01 (нет 4xx-5xx); fetch('interviewer.js') — второй запрос, не нарушает функцию, но формально не «единственный» |
| **NFR-3** Chrome/Safari/Firefox; без горизонтального скролла 1440/1024/768; 375 — без поломки | DONE | grid с `@media (max-width: 900px)` и `min-width: 0` на ячейках; signal-grid и marker-row с `@media (max-width: 640px)` | GS-06 (три вьюпорта 1440/1024/768, overflow ≤ 1px) |
| **NFR-4** Контент задач не дублируется (рендер из tasks.json); SEGMENTS/CRITERIA помечены SYNC-CONTRACT; дата актуальности в шапке | DONE | Задачи только из tasks.json через fetch. SYNC-CONTRACT: `guide.html:258–259`, `guide.html:444–445`; аналогично в interviewer.js:12–13, 24–25. Дата: `guide.html:212` «процессная часть актуальна на 2026-07-12». CLAUDE.md раздел «Контракты платформы интервью» фиксирует контракты | Код, CLAUDE.md |
| **NFR-5** Семантическая разметка h1–h3, якорная навигация с клавиатуры, focus-visible; контраст по токенам X5 | DONE | h1 в topbar, h2 на каждой секции, h3 для зон и flow-блоков; `:focus-visible { outline: 2px solid var(--x5-ink) }` `guide.html:23`; skip-link `guide.html:208`; nav с aria-label `guide.html:223`; section с aria-labelledby; цвет `--x5-ink` (#303145 на белом фоне — контраст > 7:1) | GS-09 (цвет тела); код |

---

## Edge Cases

| Edge Case | Статус | Доказательство | Проверка |
|---|---|---|---|
| Задача с пустым referenceSolutions (PY-01, PY-02, SD-01, AGT-01, AIP-01): карточка из evaluationNotes + сигналы; блок «эталонные решения» не показывается пустым | DONE | `guide.html:545, 580–588`: `if (hasRef)` — блок рендерится только при наличии решений. `guide.html:562–563`: для `isDiscussion` — note-rubric; `guide.html:591–601`: evaluationNotes рендерится как рубрика или заметки | Код; GS-03 (все id карточек присутствуют) |
| tasks.json недоступен/невалиден → FR-12 | DONE | см. FR-12 | Код |
| Задача без followUps/greenSignals → блок скрывается | DONE | `guide.html:605`: `if ((t.greenSignals && t.greenSignals.length) \|\| ...)` — условный рендер. `guide.html:614`: `if (t.followUps && t.followUps.length)` | Код |
| Открытие guide.html#task-XXX с несуществующим ID → без ошибки | DONE | `guide.html:636–639`: `if (location.hash)` → `document.getElementById(hash)` → `if (target) target.scrollIntoView()` — несуществующий ID просто не найдёт элемент, страница откроется без ошибки | Код |
| Печать при развёрнутой памятке: карточки не рвутся | DONE | `guide.html:195`: `.task-card { break-inside: avoid; page-break-inside: avoid; }` | GS-12 (print существует); print-preview вручную не проверено |
| Кандидат открыл guide.html по URL → принятое ограничение | DONE | Задокументировано в spec.md «Authentication & Access»; `<meta name="robots" content="noindex, nofollow">` (`guide.html:6`) как минимальная мера | Спека (принятое ограничение) |

---

## Итог: DONE 14 · PARTIAL 5 · MISSING 0

**DONE:** FR-1, FR-3, FR-5, FR-6, FR-9, FR-10, FR-12, NFR-1, NFR-3, NFR-4, NFR-5; Edge Cases (все 6 — DONE).

**PARTIAL (5) — требуют внимания:**

1. **FR-2 (PARTIAL)** — Баннер существует и сохраняется при печати, инструкция отдельного окна есть. Отсутствует отдельная print-плашка с дословной формулировкой «документ конфиденциальный — не передавать кандидатам» как самостоятельный печатный элемент (сейчас баннер сохраняется при печати, но его текст не содержит именно этой формулировки как плашки). Смотреть: `guide.html:191–200` (@media print) + `guide.html:215–220` (текст баннера).

2. **FR-4 (PARTIAL)** — Пять зон проверки, антивайб-зонды, маркеры 1/3/4 и три речевые моста — реализованы. Недостаёт: стоп-сигналы зоны как явный отдельный блок внутри каждой зоны (спека: «стоп-сигналы» — отдельный пункт для каждой из пяти зон). Сейчас маркер «звучит как 1» де-факто описывает стоп-ситуацию, но без явного заголовка «Стоп-сигналы зоны». Смотреть: `guide.html:310–383`.

3. **FR-7 (PARTIAL)** — Чеклист багов для DBG-02 и DBG-03 рендерится (`ul.bugcheck`). Однако `thresholdFrom()` (`guide.html:539–542`) не извлекает пороговую строку из evaluationNotes DBG-02 и DBG-03: порог («Сильный кандидат… находит 5+; средний — 3–4» / «Достаточно найти хотя бы 3») находится в середине строки `last` (после разделения по нумерованным пунктам), а не в `bugs.tail` — в итоге `.note-threshold` не появляется на карточке. Смотреть: `guide.html:521–542` + содержимое `evaluationNotes` DBG-02/DBG-03.

4. **FR-8 (PARTIAL)** — Критерии, стоп-строки, шкала, GO/NO-GO, Памятка-6 — реализованы корректно. Сквозные сигналы «берём/стоп» присутствуют, но вынесены в отдельную секцию `#signals`, а не размещены внутри секции «Оценка и вердикт» (`#scoring`), как требует спека. Смотреть: `guide.html:404–429` vs `guide.html:439–474`.

5. **NFR-2 (PARTIAL)** — Выполняется sync-guard: `guide.html:651` делает `fetch('interviewer.js')` — второй сетевой запрос помимо tasks.json. Функционально это не вредит (guard молчит при file://, не блокирует), но формально нарушает формулировку «единственный сетевой запрос — fetch('tasks.json')». Смотреть: `guide.html:649–667`.
