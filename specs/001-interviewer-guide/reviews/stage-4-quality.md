# Stage 4 — Quality Team, отчёт и вердикт

Дата: 2026-07-12 · QA Director: оркестратор (Opus) · субагенты: Requirements Validator (Sonnet), Test Engineer (Sonnet), Code Reviewer (Sonnet), Technical Writer (agent-pushkin).

## Requirements Validator

`requirements-matrix.md`: **DONE 14 · PARTIAL 5 · MISSING 0**. Разбор PARTIAL:

- **FR-2** (print-плашка не дословная) → исправлено: баннер теперь содержит точную формулировку «документ конфиденциальный, не передавать кандидатам».
- **FR-7** (порог не попадает на карточку) → валидатор был прав частично: статический разбор указал на реальный дефект разбора хвоста; закрыт фиксом W-1/W-2 (см. ниже), рантайм-проверка: пороги отображаются на обеих DBG-карточках.
- **FR-4** (стоп-сигналы зоны без отдельного заголовка) → принятая интерпретация: маркер «звучит как 1» каждой зоны сформулирован из стоп-сигналов источника, сквозные стоп-сигналы этапа — отдельная секция; отдельный дублирующий блок в каждой зоне раздул бы NFR-1.
- **FR-8** (сквозные сигналы отдельной секцией, а не внутри «Оценки») → принятое отклонение: секция «Сквозные сигналы» стоит перед памяткой задач по логике чтения; в print-режиме оценочный лист остаётся одним листом A4 именно благодаря выносу.
- **NFR-2** (второй fetch) → спека уточнена: SYNC-guard введён аудитом stage-2 (R-3), fetch interviewer.js локальный; формулировка NFR-2 приведена в соответствие.

## Test Engineer (независимый прогон)

**70/70, стабильно, флаков нет:** guide-smoke 15/15 · регресс пульта 44/44 · point-checks фиксов #6–#9 5/5 · доп. проверки 6/6 (деградация FR-12 с перехватом tasks.json; SYNC-guard молчит на синхронных константах; битый якорь без ошибок; print-эмуляция — топбар/TOC скрыты, баннер видим; двойной прогон стабилен).

## Code Reviewer

Все CRITICAL аудита stage-2 подтверждены закрытыми; XSS-аудит всех конкатенаций innerHTML — экранирование полное, уязвимостей нет. Два WARNING — **оба исправлены до вердикта**:

- **W-1:** `parseBugNotes` резал «(depth+1)» в пункте 3 DBG-03 как границу пункта (word boundary после «+»). Фикс: граница пункта — `(?:^|\s)\d{1,2}\)\s+`. Рантайм-верификация: DBG-02 — 8 пунктов, DBG-03 — ровно 12, «(depth+1)» цел.
- **W-2:** вычитание порога из хвоста строковым `replace` (неэкранированные regex-спецсимволы, риск дублирования). Фикс: `thresholdSplit()` — фильтрация предложений на два множества без replace.

INFO-пункты (regex VS16, дубликация esc/mdLite, слабость SEGMENTS-сторожа к смене внутренних границ) — приняты как технический долг, зафиксированы здесь.

## Technical Writer (pushkin)

PRD.md §4 — guide.html добавлен в дерево файлов + каноничная подсекция (18 строк); DESIGN.md — guide.html в область визуального контракта + 3 строки про формат документа/print; CLAUDE.md «Контракты платформы интервью» — сверена с кодом, правка не потребовалась. Токены сверены с colors_and_type.css.

## Retry

Не потребовался: findings ревью исправлены в том же цикле, финальные прогоны — guide-smoke 15/15 (рендер 140 мс, в NFR-бюджете), регресс 44/44.

## Вне scope — заведено отдельно

Эмодзи в `::marker` пульта + эмодзи-префиксы в данных greenSignals/redSignals tasks.json (аудит F-7, обнаружено в stage-3) — отдельный тикет после закрытия пайплайна.

## Вердикт: **PASS**

Все юниты done, требования покрыты (после фиксов и зафиксированных интерпретаций), тесты зелёные и стабильные, CRITICAL/WARNING закрыты, документация синхронизирована.
