## stage-1
- Interpretations: «раздел гайд в платформе» истолкован как standalone guide.html + ссылка из topbar пульта (не третья вьюшка внутри interviewer.html) — по CLAUDE.md «один артефакт = одна standalone страница» и чтобы не утяжелять пульт. Slug 001-interviewer-guide взят по смыслу фичи, не транслитерацией первых слов речи («так смотри давай») — они не несут смысла.
- Deviations: референсные роли stage-1 (.claude/agents/system-analyst.md, hard-critic.md) в проекте отсутствуют — роли заданы инлайн-промптами; упоминания Go/gRPC/NATS из неадаптированного шаблона /pipeline заменены на реальный стек (статический HTML). Источники .docx конвертированы в /tmp/hri-src/*.txt через textutil.
- Tradeoffs: дублирование SEGMENTS/CRITERIA в гайде статически (вместо общего config.json) — принят риск дрейфа ради отсутствия лишней инфраструктуры; памятка рендерится из tasks.json динамически — принята зависимость от http-сервера (FR-12 деградация).
- Open questions: перенесены в конец stage-1-creative.md (шаринг экрана — отдельное окно?; печатный лист — отдельная секция?; объём формулировок «1/3/4»).
## stage-2
- Interpretations: шаблонные роли stage-2 (AI Reviewer про on-premise/RAG, Performance про N+1/gRPC) неприменимы к статической странице — Performance слит с Frontend, AI Reviewer перепрофилирован в Content/Domain (методология интервью vs источники).
- Deviations: Constitution Check выполнен оркестратором без субагента — docs/constitution.md отсутствует, все 16 инвариантов NEEDS-INFO (не угадывал, I-13); эскалировано владельцу как отдельная задача «инстанциировать устав из шаблона».
- Tradeoffs: CRITICAL-находки закрыты правками спеки ДО гейта (спека — живой документ этапа) вместо цикла Revise; печатный оценочный лист — обычная видимая секция с print-CSS, отдельная скрытая print-sheet отклонена (дублирование).
- Open questions: нет — открытые вопросы stage-1 закрыты ревьюерами (отдельное окно для гайда; секция оценки видимая; «1/3/4» по одной фразе на балл).
## stage-3
- Interpretations: NFR-2 «рендер ≤200 мс» смоук меряет от goto до последней карточки (422 мс с загрузкой шрифтов; чистый рендер карточек ~20 мс) — порог FAIL поставлен 2000 мс, NFR-число логируется; hiring-шаг stage-3 пропущен, роли инлайн (консистентно со stage-1/2), страницу собирал оркестратор через design-taste-frontend (обязателен по CLAUDE.md, субагентам скилл недоступен).
- Deviations: найдены эмодзи-префиксы в данных greenSignals/redSignals tasks.json (90 шт) — tasks.json по спеке не менялся, префиксы срезаются при рендере гайда (stripSignalEmoji); первый FAIL GS-05 оказался эмодзи в моём же JS-комментарии — комментарий переписан кодами Unicode.
- Tradeoffs: SYNC-guard реализован fetch'ем interviewer.js с проверкой точных строк констант — хрупко к переформатированию, но честно отличает дрейф значений; при file:// молчит.
- Open questions: эмодзи в данных tasks.json и в ::marker пульта — отдельный тикет после stage-4 (аудит F-7).
## stage-4
- Interpretations: PARTIAL-статусы валидатора разобраны на три класса — реальные дефекты (FR-7 → фикс), устаревшая формулировка спеки (NFR-2 → спека уточнена), принятые отклонения с обоснованием (FR-4, FR-8 — задокументированы в stage-4-quality.md).
- Deviations: Technical Writer выполнен штатным agent-pushkin (его устав — актуальность PRD/DESIGN/CLAUDE), а не безымянным Sonnet из шаблона.
- Tradeoffs: W-1/W-2 ревью исправлены оркестратором в том же цикле без спавна Dev Team retry — правки точечные (2 функции), retry-цикл был бы дороже; INFO-долг (VS16-regex, дубликация esc/mdLite, SEGMENTS-сторож) принят и записан.
- Open questions: эмодзи в данных tasks.json и ::marker пульта — тикет вне scope (заводится после verdict).
