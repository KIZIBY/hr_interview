# OS
.DS_Store

# Agent-team framework — генерируемые артефакты (руками не править, в git не коммитятся)
docs/pipeline-dashboard.html
docs/fleet-dashboard.html
tests/agent-evals/last-run.json

# локальные секреты/токены (никогда не в репо)
*.token

# персональные локальные настройки Claude Code (не общие для команды)
.claude/settings.local.json

# дистрибутив платформы (собирается по запросу, не хранится в git)
fde-interview-platform.zip
